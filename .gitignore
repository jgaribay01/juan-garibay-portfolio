node_modules/
# Session-local Claude Code config, was ignored before git existed here.
.claude/
.DS_Store
*.log
# Vercel project link: contains project and org ids, not needed to build.
.vercel
# Screenshots taken during verification runs.
img/_*
# Unsubset originals, kept only while verifying the subset.
css/fonts/full/
